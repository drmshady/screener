"""Advisor-prompt assembly (feature 004).

Builds a self-contained, deterministic text prompt for one strategy candidate.
This package only *assembles* a prompt from already-computed data; it makes no
LLM call and adds no new data source.
"""
